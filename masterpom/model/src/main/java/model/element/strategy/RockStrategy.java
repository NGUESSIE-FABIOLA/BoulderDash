/**
 * 
 */
package model.element.strategy;

import imodel.IMap;
import imodel.IMobile;

/**
 * @author Heidy Kengne
 *
 */
public class RockStrategy extends FallingObjectsStrategy {

	@Override
	public void followStrategy(IMobile currentPawn, IMap map) {
		super.followStrategy(currentPawn, map);

	}

}
